import { describe, it } from 'vitest'

describe.skip('SiteWorkspaceAdapter Compatibility', () => {
  it.todo('is fully compatible with legacy v1 adapters lacking a health field')
  it.todo('gracefully defaults missing capabilities arrays for older configurations')
})
