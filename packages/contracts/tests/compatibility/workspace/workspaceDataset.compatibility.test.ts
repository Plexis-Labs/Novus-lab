import { describe, it } from 'vitest'

describe.skip('WorkspaceDataset Compatibility', () => {
  it.todo('is fully compatible with legacy v1 un-versioned datasets')
  it.todo('gracefully handles missing coverage metrics from older SDKs')
})
