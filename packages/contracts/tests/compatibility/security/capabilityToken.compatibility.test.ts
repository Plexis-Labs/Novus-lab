import { describe, it } from 'vitest'

describe('CapabilityToken Compatibility', () => {
  it.todo('should validate backward compatibility with v1 tokens')

  it.todo('should reject unsupported schema versions')

  it.todo('should support future migration tests')
})
