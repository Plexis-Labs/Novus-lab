import { describe, it } from 'vitest'

describe.skip('FeatureManifest Contract', () => {
  it.todo('strictly matches the generated JSON Schema for the AI Gateway')
  it.todo('maintains backward compatibility with v1 platform parsers')
})
