import { describe, it } from 'vitest'

describe.skip('WorkspaceDataset Contract', () => {
  it.todo('strictly matches the generated JSON Schema for the AI Gateway')
  it.todo('maintains backward compatibility with v1 parsers')
})
