import { describe, it } from 'vitest'

describe.skip('DatasetProvenance Contract', () => {
  it.todo('strictly matches the generated JSON Schema for the AI Gateway')
  it.todo('maintains backward compatibility with v1 provenance structures')
})
