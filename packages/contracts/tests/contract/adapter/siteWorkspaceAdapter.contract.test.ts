import { describe, it } from 'vitest'

describe.skip('SiteWorkspaceAdapter Contract', () => {
  it.todo('strictly matches the generated JSON Schema for the AI Gateway')
  it.todo('successfully validates untrusted payloads originating from the Python SDK')
})
