import { describe, it } from 'vitest'

describe.skip('FeatureManifest Security', () => {
  it.todo('prevents arbitrary file execution via malicious entryPoint paths')
  it.todo('strictly limits requested capabilities to prevent privilege escalation')
})
