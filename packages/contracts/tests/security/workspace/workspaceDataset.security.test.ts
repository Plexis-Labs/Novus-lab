import { describe, it } from 'vitest'

describe.skip('WorkspaceDataset Security', () => {
  it.todo('strips sensitive PII from metadata payload before serialization')
  it.todo('prevents prototype pollution within the dynamic records array')
})
