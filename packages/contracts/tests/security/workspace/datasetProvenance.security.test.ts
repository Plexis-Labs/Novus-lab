import { describe, it } from 'vitest'

describe.skip('DatasetProvenance Security', () => {
  it.todo('prevents SSRF injections by strictly validating the source URL scheme')
  it.todo('sanitizes warning strings to prevent log injection attacks')
})
