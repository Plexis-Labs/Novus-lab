import { describe, it } from 'vitest'

describe.skip('SiteWorkspaceAdapter Security', () => {
  it.todo('prevents prototype pollution within the dynamic routes array')
  it.todo('rejects unauthorized or malformed capability requests')
  it.todo('sanitizes entity strings to prevent injection attacks during extraction')
})
