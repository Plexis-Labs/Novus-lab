import { describe, it } from 'vitest'

describe.skip('RouteAdapter Security', () => {
  it.todo('prevents prototype pollution in extract selectors')
  it.todo('sanitizes event names in observe payload')
})
